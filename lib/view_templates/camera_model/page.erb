<!DOCTYPE html>
<html>
<head>
  <title><%= camera_model.name %></title>
  <%= include_basic_css %>
</head>
<body>
  <header>
    <h1><%= camera_model.name %></h1>
    <nav>
      <a href="#" class="prev">Previous page |</a>
      <a href="index.html">All Camera Makes |</a>
      <a href="camera_make_<%= camera_model.camera_make.id %>.html">All <%= camera_model.camera_make.name %> Models</a>
      <a href="#" class="next"> | Next page</a>
    </nav>
  </header>
  
  <% camera_model.works.each.with_index do |work, index| %>
    <% page = (index/per_page)  %> 
    <ul>
      <li class="<%= page == 0 ? 'active' : 'inactive'  %>" data-page="<%= page %>" >
        <img src="<%= work.thumbnail_url %>"/>
      </li>
    </ul>  
   <% end %>
  <%= include_jquery_js %>
  <%= include_pagination_js %>
</body>
</html>