
describe 'PageGenerator' do 


	describe '#generate_camera_models' do 
		#let(:generator){  PageGenerator.new('output_folder') }
		it 'should generate the correct amount of camera model pages' #do 
			#mock1, mock2, mock3 = (1..3).collect{ mock_model( "CameraModel" )}
			#expect(CameraModel).to receive(:find_each).and_yield(mock1).and_yield(mock2).and_yield(mock3)
			#expect(generator).to receive(:write).exactly(3).times
			#generator.generate_camera_models 
		#end
	end
	it 'should generate the correct amount of camera make pages' 
	it 'should generate the index page'


end